# 日報用カスタム指示

ここに、現在カスタムGemへ登録している「指示」をコピーしてください。
このファイルを `config/claude-instructions.md` に複製すると、CLI版の日報生成時に
Claudeへの追加指示として読み込まれます。

例:

- 社内のdailyチャンネル向けに、簡潔で自然な日本語にする
- 入力にない成果や進捗を推測で追加しない
- タスク名は読み手に意味が伝わる範囲で短く整える
- 「ひとこと」は本人が入力するため、AIは生成・補完・校正しない
- 誇張した表現や定型的な挨拶は使わない
